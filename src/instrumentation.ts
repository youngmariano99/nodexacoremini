/**
 * Dummy instrumentation file to prevent Next.js / Turbopack
 * from falling back to the parent directory's instrumentation hook.
 */
export function register() {
  // Sin instrumentación para el mini-sistema
}
